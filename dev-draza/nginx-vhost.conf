server {
	listen 80 default_server;
	listen [::]:80 default_server;
	server_name _;
        access_log off;
        error_log /dev/null;
	return 301 https://$host$request_uri;
}

server {
        listen 80;
        server_name www.exs.lv;
        access_log off;
        error_log /dev/null;
	return 301 https://exs.lv$request_uri;
}

server {
        listen 80;
        server_name www.coding.lv;
        access_log off;
        error_log /dev/null;
        return 301 https://coding.lv$request_uri;
}

server {
        listen 80;
        server_name rs.exs.lv;
        access_log off;
        error_log /dev/null;
        return 301 https://runescape.exs.lv$request_uri;
}

server {
        listen 443 ssl http2;

	root /home/www/exs.lv;
        index index.php;

	add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

        server_name exs.lv coding.lv www.exs.lv rp.exs.lv www.coding.lv rs.exs.lv runescape.exs.lv lol.exs.lv static.exs.lv secure.exs.lv;
        client_max_body_size 20M;

        #SSL
        ssl_certificate /etc/letsencrypt/live/exs.lv/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/exs.lv/privkey.pem;

	ssl_protocols        SSLv3 TLSv1 TLSv1.1 TLSv1.2;	# default on newer versions
	ssl_ciphers          ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-RC4-SHA:ECDHE-RSA-AES128-SHA:RC4-SHA:RC4-MD5:ECDHE-RSA-AES256-SHA:AES256-SHA:ECDHE-RSA-DES-CBC3-SHA:DES-CBC3-SHA:AES128-SHA;
	ssl_prefer_server_ciphers on;

#	if ($http_user_agent ~* (SentiBot|AddThis.com) ) {
#		return 444;
#	}

        location / {

	        if (-f $request_filename) {
			break;
		}
		if (!-f $request_filename) {
			rewrite ^/(.+)$ /index.php?fakeurl=$1 last;
			break;
		}

        }

        location /api {
		return 301 https://exs.lv/api/;
        }

        location /api/ {
		rewrite ^/api/(.*)$ /modules/api/index.php?params=$1 last;
        }

        location ~ /dati/bildes/topic-av/(.*\.jpg) {
                expires 1d;
                try_files $uri /index.php?viewcat=page-avatars&img=$1;
        }

        location ~* \.(png|jpg|jpeg|gif|ico)$ {
                expires 1y;
        }

        location /js/ {
		expires 3h;
                rewrite ^/js/(.*\.js) /combine.php?type=javascript&files=$1;
        }

        location /css/ {
		expires 3h;
                rewrite ^/css/(.*\.css) /combine.php?type=css&files=$1;
        }

        location ~ \.php$ {
               include snippets/fastcgi-php.conf;
               fastcgi_pass unix:/run/php/php7.1-fpm.sock;
        }

        location ~ /\.ht {
                deny all;
        }

        location ~ /\.tpl {
                deny all;
        }

}

server {
        listen 443 ssl http2;

        root /home/www/m.exs.lv;
        index index.php;

        server_name m.exs.lv mrs.exs.lv mrp.exs.lv m.coding.lv mlol.exs.lv mrunescape.exs.lv;
        client_max_body_size 20M;

	add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

        #SSL
        ssl_certificate /etc/letsencrypt/live/exs.lv/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/exs.lv/privkey.pem;   

	ssl_protocols        SSLv3 TLSv1 TLSv1.1 TLSv1.2;     # default on newer versions
	ssl_ciphers          ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-RC4-SHA:ECDHE-RSA-AES128-SHA:RC4-SHA:RC4-MD5:ECDHE-RSA-AES256-SHA:AES256-SHA:ECDHE-RSA-DES-CBC3-SHA:DES-CBC3-SHA:AES128-SHA;
	ssl_prefer_server_ciphers on;

        location / {

                if (-f $request_filename) {
                        break;
                }
                if (!-f $request_filename) {
                        rewrite ^/(.+)$ /index.php?fakeurl=$1 last;
                        break;
                }

        }

        location ~ /av/(.*\.*) {
                expires 1d;
                try_files $uri /index.php?viewcat=mav&img=$1;
        }

        location ~* \.(png|jpg|jpeg|gif|ico)$ {
                expires 1y;
        }

        location ~ \.php$ {
               include snippets/fastcgi-php.conf;
               fastcgi_pass unix:/run/php/php7.1-fpm.sock;
        }

        location ~ /\.ht {
                deny all;
        }

        location ~ /\.tpl {
                deny all;
        }

}

server {
        listen 443 ssl http2;

        root /home/www/api.exs.lv;
        index index.php;

	add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

        server_name api.exs.lv android.exs.lv ios.exs.lv;
        client_max_body_size 20M;

        #SSL
        ssl_certificate /etc/letsencrypt/live/exs.lv/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/exs.lv/privkey.pem;   

        ssl_protocols        SSLv3 TLSv1 TLSv1.1 TLSv1.2;     # default on newer versions
        ssl_ciphers          ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-RC4-SHA:ECDHE-RSA-AES128-SHA:RC4-SHA:RC4-MD5:ECDHE-RSA-AES256-SHA:AES256-SHA:ECDHE-RSA-DES-CBC3-SHA:DES-CBC3-SHA:AES128-SHA;
        ssl_prefer_server_ciphers on;

        location / {

                if (-f $request_filename) {
                        break;
                }
                if (!-f $request_filename) {
                        rewrite ^/(.+)$ /index.php?params=$1 last;
                        break;
                }

        }

        location ~* \.(png|jpg|jpeg|gif|ico)$ {
                expires 1y;
        }

        location ~ \.php$ {
               include snippets/fastcgi-php.conf;
               fastcgi_pass unix:/run/php/php7.1-fpm.sock;
        }

        location ~ /\.ht {
                deny all;
        }

        location ~ /\.tpl {
                deny all;
        }

}


server {
        listen 443 ssl http2;

        root /var/www/munin;
        index index.html;
        server_name munin.exs.lv;

	add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

        #SSL
        ssl_certificate /etc/letsencrypt/live/exs.lv/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/exs.lv/privkey.pem;   

	ssl_protocols        SSLv3 TLSv1 TLSv1.1 TLSv1.2;     # default on newer versions
	ssl_ciphers          ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-RC4-SHA:ECDHE-RSA-AES128-SHA:RC4-SHA:RC4-MD5:ECDHE-RSA-AES256-SHA:AES256-SHA:ECDHE-RSA-DES-CBC3-SHA:DES-CBC3-SHA:AES128-SHA;
	ssl_prefer_server_ciphers on;

        location / {

                if (-f $request_filename) {
                        break;
                }

        }

        location ~* \.(png|jpg|jpeg|gif|ico)$ {
                expires 1m;
        }

        location ~ /\.ht {
                deny all;
        }
}


server {
        listen 443 ssl http2;

        root /home/www/img.exs.lv;
        index index.php;

	add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

        access_log off;
        error_log /dev/null;

        server_name img.exs.lv;

        #SSL
        ssl_certificate /etc/letsencrypt/live/exs.lv/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/exs.lv/privkey.pem;   

	ssl_protocols        SSLv3 TLSv1 TLSv1.1 TLSv1.2;     # default on newer versions
	ssl_ciphers          ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-RC4-SHA:ECDHE-RSA-AES128-SHA:RC4-SHA:RC4-MD5:ECDHE-RSA-AES256-SHA:AES256-SHA:ECDHE-RSA-DES-CBC3-SHA:DES-CBC3-SHA:AES128-SHA;
	ssl_prefer_server_ciphers on;

        location = / {
                return 301 https://exs.lv/img;
        }

        location ~* ^/userpic/large/.+\.(gif)$ {
                expires 1y;
                add_header Vary Accept;
                try_files $uri$webp_suffix $uri =404;
        }

        location ~* ^/userpic/medium/.+\.(gif)$ {
                expires 1y;
                add_header Vary Accept;
                try_files $uri$webp_suffix $uri =404;
        }

        location ~* ^/userpic/small/.+\.(gif)$ {
                expires 1y;
                add_header Vary Accept;
                try_files $uri$webp_suffix $uri =404;
        }

        location ~* \.(png|jpg|jpeg|gif|ico)$ {
                expires 1y;
        }

}

server {
        listen 80;

        root /home/www/img.exs.lv;
        index index.php;

        access_log off;
        error_log /dev/null;

        server_name img.exs.lv;

        location = / {
                return 301 https://exs.lv/img;
        }

        location ~* \.(png|jpg|jpeg|gif|ico)$ {
                expires 1y;
        }
}

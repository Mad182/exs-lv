<?
exit;
set_time_limit(600);



$users = $db->get_results("SELECT id,nick,lastseen,mail FROM users WHERE `lastseen` > '".date('Y-m-d H:i:s',time()-2629743)."' ORDER BY `lastseen` DESC");
$i=0;
foreach($users as $user) {
	$text = '<p>Čau!<br />Ja ir brīvs brīdis, lūdzu aptauju - <a href="http://s.exs.lv/6cj">http://s.exs.lv/6cj</a>, un piedalies telefona izlozē.<br />Tā ir tele2 rīkota akcija.</p>
<p>PS.<br />Jā, šī ir reklāma, bet iespēja laimēt ir īsta un aptauja aizņems tikai pāris minūtes :)</p>';

	$db->query("INSERT INTO pm (from_uid,to_uid,date,ip,title,text,is_read) VALUES ('1','$user->id',NOW(),'127.0.0.1','Aizpildi aptauju un laimē telefonu!','$text','0')");
	notify($user->id,9);
	echo $i . '. - ' . $user->nick . ' - pm ';
	echo '<br />';
	$i++;
}



?>